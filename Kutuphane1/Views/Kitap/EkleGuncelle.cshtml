@model Kitap
@{
    ViewData["Title"] = "Ekle Güncelle";
}

<form method="post" enctype="multipart/form-data">
    <input asp-for="ResimUrl" hidden />
    @* <div asp-validation-summary="ModelOnly" >

    </div> *@
    <div class="row">
        <div class="col-10">
            <div class=" container border p-3 mt-5" style="background-color:lightyellow">
                <div class="row pb-3">
                    <h2 class="text-bg-primary">@(Model == null ? "Yeni Kitap Ekle" : "Kitap Güncelle")</h2>
                </div>
                <div class="mb-3">
                    <label asp-for="KitapAdi" class="p-0"></label>
                    <input asp-for="KitapAdi" class="form-control" />
                    <span asp-validation-for="KitapAdi" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="KitapTuruId" class="p-0"></label>
                    <select asp-for="KitapTuruId" asp-items="@ViewBag.KitapTuruList" class="form-select"></select>
                </div>
                <div class="mb-3">
                    <label asp-for="Tanim" class="p-0"></label>
                    <input asp-for="Tanim" class="form-control" />
                    <span asp-validation-for="Tanim" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="Yazar" class="p-0"></label>
                    <input asp-for="Yazar" class="form-control" />
                    <span asp-validation-for="Yazar" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="Fiyat" class="p-0"></label>
                    <input asp-for="Fiyat" class="form-control" />
                    <span asp-validation-for="Fiyat" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="ResimUrl" class="p-0"></label>
                    <input   type="file" name="file" class="form-control" />
                </div>
               

                @if (Model != null)
                {
                    <button type="submit" class="btn btn-lg btn-primary" style="width:250px">Güncelle</button>
                }
                else
                {
                    <button type="submit" class="btn btn-lg btn-primary" style="width:250px">Kitap Ekle</button>
                }


                @*  <button asp-controller="KitapTuru" asp-action="Index" class="btn btn-lg btn-primary" style="width:250px">geri dön</button> *@
                <a asp-controller="Kitap" asp-action="Index" class="btn btn-lg btn-primary" style="width:250px">Geri Dön</a>
            </div>
        </div>
        <div class="col-2" >
            @if (Model != null)
            {
                <img src="@Model.ResimUrl" width="100%" style="border-radius:7px; border:1px solid #bbb9b9" />
            }
           
        </div>
    </div>

    
   
</form>

@* <script>
    // Get a reference to our file input
    const fileInput = document.querySelector('input[type="file"]');
    let filePath = document.getElementById("ResimUrl");
    console.log(filePath+"abcaddsa")
    // Create a new File object
    const myFile = new File(['Hello World!'], filePath.value.replace("\\img\\", ""), {
        type: 'image/jpg',
        lastModified: new Date(),
    });

    // Now let's create a DataTransfer to get a FileList
    const dataTransfer = new DataTransfer();
    dataTransfer.items.add(myFile);
    fileInput.files = dataTransfer.files;
</script> *@

@* @section Scripts{
    @{
        <partial name ="_ValidationScriptsPartial"/>
    }
} *@